<IfModule mod_rewrite.c>
  RewriteEngine On

  # Base path
  RewriteBase /milestone/

  # Redirect all requests to index.html except actual files/folders
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [L]
</IfModule>
